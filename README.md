This is a very basic project for now..

for now, run:

```python tree.py```

TODO:

build script for executable
command line args / config
 - common large dir exclusions (node_modules, .git, etc...)
 - configurable max_depth
 - printing styles